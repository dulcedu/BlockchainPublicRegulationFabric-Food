query listProductListingContractsBySupplierID {
    description: "Select all productlistings that came from a given supplier"
    statement:
        SELECT composer.food.supply.ProductListingContract
            WHERE (supplier == _$supplierParam)
}